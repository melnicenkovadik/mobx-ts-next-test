import {FC} from "react";
import TodoContainer from "@/containers/todo";

const Home: FC = () => {
    return (
        <main>
            <TodoContainer/>
        </main>
    );
};

export default Home;
